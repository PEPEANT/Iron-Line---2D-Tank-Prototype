# 수복 (Iron Line) — AI 작업자 계약

이 리포에서 작업하는 모든 AI(Codex, GPT, Claude 등)는 이 문서를 규칙으로 삼는다. 이 문서는 낡은 `docs/archive/AGENT_HANDOFF.md`를 대체한다.

## 먼저 읽기 (순서대로)

1. `docs/INDEX.md` — 살아있는 문서 색인
2. `docs/r1-release-gate.md` — 현재 범위와 R2 우선순위. **게이트를 넓히는 수정 금지, 좁히기만 허용**
3. `docs/code-structure-rules.md` + `docs/structure-debt-ratchet-blueprint.md` — 파일 크기 규칙
4. 맡은 작업의 설계도 (INDEX에서 찾기). **설계도에 없는 작업은 하지 않는다.**

## 절대 규칙

1. **설계도가 코드와 다르면 멈춘다.** 코드를 설계도에 맞추지 말고, 발견한 불일치를 해당 md 하단에 기록하고 작업을 중단한 뒤 보고한다. 설계도도 틀릴 수 있다.
2. **검증 없이 완료 선언 금지.** 모든 세션은 `npm run check` 통과로 끝난다. AI/전투를 만졌으면 `node tools/check-battlefield-tempo.cjs`도 실행하고, **수치를 해당 설계도 하단에 기록**한다 (목표: deaths/min 6~12).
3. **핫스팟에 기능 본문 추가 금지**: main.js, infantry-ai.js, renderer.js, styles.css, hud.js, combat.js 등 1000줄+ 파일. 새 기능 = 새 모듈. 설계도의 "파일 경계" 표를 따른다.
4. **한 세션 = 한 단계.** 설계도의 단계(P1, S1 등)를 건너뛰거나 병합하지 않는다. 단계 완료 조건을 통과해야 다음 단계.
5. **범위 확장 금지.** 작업 중 좋은 아이디어가 생기면 구현하지 말고 `docs/r1-release-gate.md`의 R2 백로그에 한 줄 적는다.
6. R1 출시 후에는 버그 수정과 줄 수 감소만 main 브랜치에 — 기능 확장은 R2 브랜치.

## 분업 규칙 (2026-07-05 확정)

- 설계도에 `[Codex]` 태그가 붙은 절 = 스펙이 고정됨. 그대로 구현하면 된다.
- `[난제]` 태그 = 판단이 남아 있음. **구현하지 말고** 설계 세션(Fable/사람)으로 넘긴다.
- 태그가 없는 오래된 문서의 작업은 난이도 표기(r1-release-gate.md R2 우선순위)를 따른다: [하]·[중]=구현 위임 가능, [상]=설계 확정 후에만.

## 검증 도구 모음

- `npm run check` — 문법/중복/코드건강/에셋 (필수)
- `npm run check:online` — 온라인 흐름을 만졌을 때
- `npm run tempo` — AI/전투 밸런스를 만졌을 때 (2회 실행, 편차 큼)
- `npm run census` — AI 행동 분포

## 사용자에 대해

- 사용자는 코드를 읽지 못한다. 완료 보고는 "무엇이 화면에서 어떻게 보이는가"로 쓴다.
- 허락을 묻는 중간 질문 금지 — 이 문서와 설계도 범위 안에서는 진행하고, 범위 밖이면 백로그에 적고 멈춘다.
- 북극성: 스토리("전쟁에서 혼자가 아니다"), 원칙: 1분 시작, 텍스트 다이어트, 삭제보다 이사.
